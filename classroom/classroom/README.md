# Data Pipeline

## 📝 Цель проекта

Создать **модульный конвейер обработки данных** — систему, которая принимает массив товаров и последовательно преобразует их: фильтрует, преобразует, агрегирует, сортирует. В этом проекте вы отработаете:

- **Функциональное программирование** — чистые функции, композиция, высокоуровневые функции
- **Работа с массивами** — `map`, `filter`, `reduce` и их комбинация
- **Композиция функций** — создание пайплайна (`pipe`), который соединяет функции в цепочку
- **Трансформация данных** — фильтрация, маппинг, агрегирование в одном потоке
- **Модульность** — разделение на файлы для переиспользования

**Ключевая идея:** Вместо того чтобы писать огромную функцию, которая всё делает, вы пишете маленькие функции, которые делают одно. Затем вы соединяете их в цепочку (пайплайн) — когда результат одной функции становится входом для следующей.

```
Исходные данные
    ↓
Фильтр (только товары в наличии)
    ↓
Преобразование (добавить налог к цене)
    ↓
Группировка (по категориям)
    ↓
Результат
```

---

## 📁 Структура проекта

```
Data-Processor-Data-Pipeline/
  src/
    data.js           # массив товаров (исходные данные)
    pipeline.js       # функция pipe() для композиции
    filters.js        # функции фильтрации
    mappers.js        # функции преобразования (map)
    reducers.js       # функции агрегирования (reduce)
    processors.js     # готовые пайплайны и сортировки
    index.js          # запуск и демонстрация
  package.json
  README.md
```

---

## 📦 Модель данных

**Товар (Product):**
```javascript
{
  id: number,          // 1, 2, 3, ...
  name: string,        // "Laptop", "Mouse", "Monitor"
  price: number,       // 999, 25, 350
  category: string,    // "Electronics", "Accessories", "Furniture"
  inStock: boolean     // true | false
}
```

**Пример данных:**
```javascript
const products = [
  { id: 1, name: "Laptop", price: 999, category: "Electronics", inStock: true },
  { id: 2, name: "Mouse", price: 25, category: "Accessories", inStock: true },
  { id: 3, name: "Monitor", price: 350, category: "Electronics", inStock: false },
  { id: 4, name: "Desk", price: 200, category: "Furniture", inStock: true },
  { id: 5, name: "Chair", price: 150, category: "Furniture", inStock: false },
  { id: 6, name: "Keyboard", price: 80, category: "Accessories", inStock: true }
];
```

---

## 🎯 Задачи

### 1️⃣ `pipe` функция (композиция)

**Что нужно сделать:** Создать универсальную функцию, которая соединяет несколько функций в цепочку.

**Сигнатура:**
```javascript
const pipe = (...functions) => {
  // Ваш код здесь
};
```

**Примеры использования:**

**Простой пример (две функции):**
```javascript
const add5 = (x) => x + 5;
const multiply2 = (x) => x * 2;

const pipeline = pipe(add5, multiply2);

pipeline(3);
// 1. Применяем add5(3) → 8
// 2. Применяем multiply2(8) → 16
// → 16
```

**Сложный пример (с фильтром и преобразованием):**
```javascript
const filterInStock = (products) => products.filter(p => p.inStock);
const getNames = (products) => products.map(p => p.name);
const sortAlpha = (names) => [...names].sort();

const pipeline = pipe(filterInStock, getNames, sortAlpha);

pipeline(products);
// 1. Фильтруем: только товары в наличии
// 2. Маппим: берём только имена
// 3. Сортируем: алфавитно
// → ['Accessory 1', 'Accessory 2', 'Electronics 1', ...]
```

**Подсказка:**
- `pipe` принимает список функций как параметры (`...functions`)
- Возвращает новую функцию
- Эта новая функция должна применить все функции последовательно
- Используйте `reduce` для применения функций одна за другой
- Логика: `функции.reduce((data, fn) => fn(data), начальныеДанные)`

---

### 2️⃣ Фильтры (`filters.js`)

**Что нужно сделать:** Создать функции для фильтрации товаров.

**Задача 1: `filterByCategory`**

**Сигнатура:**
```javascript
const filterByCategory = (category) => {
  // Ваш код здесь (возвращает функцию!)
};
```

**Примеры использования:**
```javascript
// Шаг 1: Создаём фильтр для "Electronics"
const getElectronics = filterByCategory('Electronics');

// Шаг 2: Применяем фильтр к данным
getElectronics(products);
// → [
//     { id: 1, name: "Laptop", category: "Electronics", ... },
//     { id: 3, name: "Monitor", category: "Electronics", ... }
//   ]

// Другая категория
const getFurniture = filterByCategory('Furniture');
getFurniture(products);
// → [
//     { id: 4, name: "Desk", category: "Furniture", ... },
//     { id: 5, name: "Chair", category: "Furniture", ... }
//   ]
```

**Подсказка:**
- Функция возвращает ещё одну функцию (higher-order function)
- Внутренняя функция использует `.filter()` для проверки `category`
- Не мутируйте исходный массив

**Задача 2: `filterInStock`**

**Сигнатура:**
```javascript
const filterInStock = (products) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
filterInStock(products);
// → [
//     { id: 1, name: "Laptop", inStock: true, ... },
//     { id: 2, name: "Mouse", inStock: true, ... },
//     { id: 4, name: "Desk", inStock: true, ... },
//     { id: 6, name: "Keyboard", inStock: true, ... }
//   ]

console.log(products);  // исходный массив не изменился!
```

**Подсказка:**
- Используйте `.filter()` с проверкой `p.inStock === true`
- Возвращайте новый массив, не мутируйте входной

---

### 3️⃣ Преобразования (`mappers.js`)

**Что нужно сделать:** Создать функции для трансформации данных.

**Задача 1: `mapToNames`**

**Сигнатура:**
```javascript
const mapToNames = (products) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
mapToNames(products);
// → ["Laptop", "Mouse", "Monitor", "Desk", "Chair", "Keyboard"]
```

**Подсказка:**
- Используйте `.map()` для извлечения только названий
- Возвращайте новый массив

**Задача 2: `mapToPriceWithTax`**

**Сигнатура:**
```javascript
const mapToPriceWithTax = (taxRate) => {
  // Ваш код здесь (возвращает функцию!)
};
```

**Примеры использования:**
```javascript
// Шаг 1: Создаём преобразователь с налогом 20%
const addTax = mapToPriceWithTax(0.2);

// Шаг 2: Применяем к товарам
const productsWithTax = addTax(products);
// → [
//     { id: 1, name: "Laptop", price: 1198.8, ... },  // 999 * 1.2
//     { id: 2, name: "Mouse", price: 30, ... },       // 25 * 1.2
//     ...
//   ]

console.log(products[0].price);  // 999 (не изменился!)
```

**Подсказка:**
- Функция возвращает ещё одну функцию (higher-order function)
- Внутри используйте `.map()` для трансформации
- Создавайте новые объекты со спред-оператором: `{ ...product, price: ... }`
- Не мутируйте оригинальные объекты

---

### 4️⃣ Агрегирование (`reducers.js`)

**Что нужно сделать:** Создать функции для объединения/группировки данных.

**Задача 1: `sumPrices`**

**Сигнатура:**
```javascript
const sumPrices = (products) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
sumPrices(products);
// → 1804  (999 + 25 + 350 + 200 + 150 + 80)

// Только товары в наличии
const inStock = products.filter(p => p.inStock);
sumPrices(inStock);
// → 1304  (999 + 25 + 200 + 80)
```

**Подсказка:**
- Используйте `.reduce()` для суммирования
- Начальное значение: 0
- На каждом шаге: `accumulator + product.price`

**Задача 2: `groupByCategory`**

**Сигнатура:**
```javascript
const groupByCategory = (products) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
groupByCategory(products);
// → {
//     Electronics: [
//       { id: 1, name: "Laptop", category: "Electronics", ... },
//       { id: 3, name: "Monitor", category: "Electronics", ... }
//     ],
//     Accessories: [
//       { id: 2, name: "Mouse", category: "Accessories", ... },
//       { id: 6, name: "Keyboard", category: "Accessories", ... }
//     ],
//     Furniture: [
//       { id: 4, name: "Desk", category: "Furniture", ... },
//       { id: 5, name: "Chair", category: "Furniture", ... }
//     ]
//   }
```

**Подсказка:**
- Используйте `.reduce()` для группировки
- Начальное значение: `{}`
- Для каждого товара: если категория не существует в объекте, создайте массив
- Добавьте товар в массив его категории: `acc[category].push(product)`
- Проверьте `acc[category]` перед `push` или используйте `acc[category] = acc[category] || []`

---

### 5️⃣ Сортировки (`processors.js`)

**Что нужно сделать:** Создать функции для сортировки товаров.

**Задача 1: `sortByPriceAsc`**

**Сигнатура:**
```javascript
const sortByPriceAsc = (products) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
sortByPriceAsc(products);
// → [
//     { id: 2, name: "Mouse", price: 25, ... },
//     { id: 6, name: "Keyboard", price: 80, ... },
//     { id: 5, name: "Chair", price: 150, ... },
//     { id: 4, name: "Desk", price: 200, ... },
//     { id: 3, name: "Monitor", price: 350, ... },
//     { id: 1, name: "Laptop", price: 999, ... }
//   ]

console.log(products);  // исходный массив не изменился!
```

**Подсказка:**
- Создайте копию массива (`.slice()` или спред-оператор `[...]`)
- Используйте `.sort()` на копии
- Для сортировки по цене: `a.price - b.price`

**Задача 2: `sortByPriceDesc`**

**Сигнатура:**
```javascript
const sortByPriceDesc = (products) => {
  // Ваш код здесь
};
```

**Примеры использования:**
```javascript
sortByPriceDesc(products);
// → [
//     { id: 1, name: "Laptop", price: 999, ... },
//     { id: 3, name: "Monitor", price: 350, ... },
//     { id: 4, name: "Desk", price: 200, ... },
//     { id: 5, name: "Chair", price: 150, ... },
//     { id: 6, name: "Keyboard", price: 80, ... },
//     { id: 2, name: "Mouse", price: 25, ... }
//   ]
```

**Подсказка:**
- Аналогично `sortByPriceAsc`, но обратный порядок
- Для обратного порядка: `b.price - a.price`

---

### 6️⃣ Готовые пайплайны (`processors.js`)

**Что нужно сделать:** Создать минимум 3 полноценных пайплайна, которые используют `pipe` и другие функции.

**Пример структуры:**
```javascript
// Пайплайн 1: только названия товаров в наличии, отсортированные
export const getNamesInStock = () => {
  return pipe(
    filterInStock,           // фильтруем
    mapToNames,              // берём названия
    (names) => [...names].sort()  // сортируем
  );
};

// Использование
const result = getNamesInStock()(products);
```

**Что должны делать пайплайны:**
1. **Пайплайн 1:** Товары в наличии → названия → отсортировано
2. **Пайплайн 2:** Только Electronics → с налогом 15% → отсортировано по цене
3. **Пайплайн 3:** Все товары → сгруппировано по категориям → посчитана сумма по каждой категории

**Подсказка:**
- Используйте `pipe()` для последовательного применения функций
- Каждый пайплайн должен комбинировать несколько трансформаций
- Экспортируйте готовые функции для использования в `index.js`

---

### 7️⃣ `index.js` демонстрация

**Что нужно сделать:** Запустить все пайплайны и показать результаты в консоли.

**Примеры вывода:**
```
1️⃣ Товары в наличии:
   ['Keyboard', 'Laptop', 'Mouse', 'Desk']

2️⃣ Electronics с налогом (15%):
   Laptop: $1148.85
   Monitor: $402.50

3️⃣ Группировка по категориям:
   Electronics: 2 товара, $1349
   Accessories: 2 товара, $105
   Furniture: 2 товара, $350
```

**Подсказка:**
- Импортируйте все пайплайны из `processors.js`
- Импортируйте данные из `data.js`
- Вызовите каждый пайплайн и выведите результаты красиво

---

## 🛠️ Общие требования

### Функциональное программирование

- ✅ **ИСПОЛЬЗУЙТЕ:** `map`, `filter`, `reduce` для трансформации
- ✅ **ИСПОЛЬЗУЙТЕ:** стрелочные функции
- ✅ **ИСПОЛЬЗУЙТЕ:** higher-order functions (функции, возвращающие функции)
- ✅ **ИСПОЛЬЗУЙТЕ:** спред-оператор `[...]` и `{ ...obj }`
- ✅ **ИСПОЛЬЗУЙТЕ:** чистые функции (без побочных эффектов)
- ❌ **НЕ ИСПОЛЬЗУЙТЕ:** `for` и `while` циклы
- ❌ **НЕ ИСПОЛЬЗУЙТЕ:** мутирование данных (`.push()`, `.sort()` напрямую)
- ❌ **НЕ ИСПОЛЬЗУЙТЕ:** `var`

### Модульность

- ✅ **ИСПОЛЬЗУЙТЕ:** отдельные файлы для разных типов функций
- ✅ **ИСПОЛЬЗУЙТЕ:** `export` и `import`
- ✅ **ИСПОЛЬЗУЙТЕ:** descriptive названия (понятно, что делает каждая функция)
- ✅ **ИСПОЛЬЗУЙТЕ:** одну ответственность на функцию (Single Responsibility)

---

## 💡 Полезные советы для новичков

### Что такое pipe (конвейер)?

**Аналогия в реальности:**

Представьте конвейер на заводе:
1. Сырьё приходит на вход
2. На станции 1 оно преобразуется
3. Результат идёт на станцию 2
4. Там опять преобразуется
5. И так далее...
6. В конце — готовый продукт

**В коде:**
```javascript
pipe(station1, station2, station3)(rawData)

// Эквивалентно:
station3(station2(station1(rawData)))

// Но читается слева направо, как в реальности!
```

### Map, Filter, Reduce — в жизни

**Пример с покупками:**

```javascript
const purchases = [
  { item: 'Apple', price: 2 },
  { item: 'Banana', price: 1 },
  { item: 'Orange', price: 3 }
];
```

**MAP — преобразование:**
```javascript
// "Преобразуем" покупки в цены
purchases.map(p => p.price)
// → [2, 1, 3]
```

**FILTER — отбор:**
```javascript
// "Фильтруем" дорогие товары (> 2)
purchases.filter(p => p.price > 2)
// → [{ item: 'Orange', price: 3 }]
```

**REDUCE — объединение:**
```javascript
// "Суммируем" все цены
purchases.reduce((sum, p) => sum + p.price, 0)
// → 6
```

### Чистые функции (Pure Functions)

**Что значит "чистая функция"?**

Функция чистая, если:
1. Одинаковый вход → одинаковый выход (всегда!)
2. Нет побочных эффектов (не меняет внешнее состояние)

```javascript
// ❌ Грязная (мутирует входные данные)
const addTaxMutating = (products, tax) => {
  products.forEach(p => {
    p.price = p.price * (1 + tax);  // ← мутирует!
  });
  return products;
};

// ✅ Чистая (создаёт новый массив)
const addTaxPure = (products, tax) => {
  return products.map(p => ({
    ...p,
    price: p.price * (1 + tax)
  }));
};
```

**Почему это важно?**
- Предсказуемость — функция не имеет скрытых побочных эффектов
- Тестируемость — легко тестировать
- Переиспользуемость — можно использовать везде без страха
- Отладка — если что-то сломалось, причина в самой функции

### Reduce в деталях

**Как работает reduce:**

```javascript
const numbers = [1, 2, 3, 4];

numbers.reduce((accumulator, current) => {
  console.log(`acc: ${accumulator}, current: ${current}`);
  return accumulator + current;
}, 0);  // ← начальное значение

// Шаг 1: acc = 0, current = 1  → возвращаем 1
// Шаг 2: acc = 1, current = 2  → возвращаем 3
// Шаг 3: acc = 3, current = 3  → возвращаем 6
// Шаг 4: acc = 6, current = 4  → возвращаем 10
// Результат: 10
```

**Для группировки:**

```javascript
const products = [
  { category: 'A', name: 'Item1' },
  { category: 'B', name: 'Item2' },
  { category: 'A', name: 'Item3' }
];

products.reduce((grouped, product) => {
  const cat = product.category;
  // Если категории нет, создаём пустой массив
  if (!grouped[cat]) grouped[cat] = [];
  // Добавляем товар
  grouped[cat].push(product);
  return grouped;
}, {});

// Результат:
// {
//   A: [{category: 'A', name: 'Item1'}, {category: 'A', name: 'Item3'}],
//   B: [{category: 'B', name: 'Item2'}]
// }
```

### Не мутируйте данные!

**❌ Неправильно (мутирует):**
```javascript
const original = [3, 1, 2];
const sorted = original.sort();  // original тоже изменился!
console.log(original);  // [1, 2, 3] ← изменился!
```

**✅ Правильно (создаёт копию):**
```javascript
const original = [3, 1, 2];
const sorted = [...original].sort();  // копия
console.log(original);  // [3, 1, 2] ← не изменился!
```

### Higher-order functions (функции, возвращающие функции)

**Зачем это нужно?**

Позволяет создавать гибкие, переиспользуемые функции.

```javascript
// ❌ Жёсткая функция (только для одной категории)
const getElectronics = (products) => {
  return products.filter(p => p.category === 'Electronics');
};

// ✅ Гибкая функция (для любой категории)
const filterByCategory = (category) => {
  return (products) => {
    return products.filter(p => p.category === category);
  };
};

// Используем:
const getElectronics = filterByCategory('Electronics');
const getFurniture = filterByCategory('Furniture');

getElectronics(products);  // → товары Electronics
getFurniture(products);    // → товары Furniture
```

---

## 🎓 Что вы узнаете, делая этот проект

✅ Как работает **функциональное программирование**  
✅ Как использовать **map, filter, reduce** в реальных задачах  
✅ Как писать **чистые функции** без побочных эффектов  
✅ Как создавать **higher-order functions** для гибкости  
✅ Как создавать **композицию функций** (pipe)  
✅ Как трансформировать **сложные данные** (фильтрация, группировка, агрегация)  
✅ Как делать **модульный, переиспользуемый код**  
✅ Как думать о **конвейерах обработки данных**  

**Успехов! 🚀**
